# Minimal runtime requirements (only packages not included in the Python standard library)
pandas>=1.5.0
requests>=2.0

# Optional
# python-ldap  -> requires libldap2-dev, libsasl2-dev (Debian/Ubuntu) -> Does not work on windows
# thonny       -> only needed to run the Thonny plugin
